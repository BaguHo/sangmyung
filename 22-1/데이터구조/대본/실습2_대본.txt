안녕하세요 201921009 엄지호입니다.

먼저 1번문제에서 대소문자를 판결하기위해서 
strchr을 사용하여 특수문자 유무를 판단하였습니다.
그리고 isupper함수와 islower함수를 반복문과 temp변수를 사용하여 대소문자가 다 있는지를
확인하였습니다. 
마지막으로 strlen을 통해서 길이가 10 이하인 경우까지 체크해줬습니다.

2번 문제는 복소수를 표현하기위해서 double형 2개로 구조체를 만들었습니다.
몇 개의 복소수를 입력할 지 물어보고 동적할당으로 배열을 만들어주었습니다.
그리고 실수 부분과 허수부분을 나눠서 입력받아 순환 함수 sum으로 배열을 넘겨줍니다.
sum 함수에서는 n을 통해서 입력받은 복소수의 수만큼만 순환함수가 돌아가게 하였고,
x, y 값에 실수부분과 허수부분을 더하는 방식으로 하였습니다.

3번 문제는 문자열을 입력받은 후 length함수로 문자열을 넘긴 후 문자열의 길이를 세는 
변수  i를 통해서 문자열의 길이를 구합니다.

4번은 
정렬되어 있는 배열의 값 중에서 찾고 싶은 수를 입력하고 그 수를 target 변수에 넣어서
순환함수에 있는 mid 변수와 비교를 합니다.
input[mid]가 target보다 클 경우, 순환함수 high에 mid - 1을 대입하고
input[mid]가 target보다 작을 경우, 순한함수 low에 mid + 1을 대입합니다.
그리고 input[mid]가 target과 같을 경우에 mid값을 반환하고 함수를 종료합니다.
예외처리로 찾으러고 하는 수가 배열에 없는 경우에는 순환함수를 돌다가 
low 가 high보다 커지게 됩니다. 이 경우에 return -1을 합니다. 
그리고 찾는 수가 배열에 없다는 내용을 출력하는게 기존 코드와의 변경점입니다.
 이상입니다.
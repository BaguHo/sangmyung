접근제한자를 이용한 encapsulation
 public(모든 곳에서 접근 가능) prodeded privte(내부 클래스에서만 접근 가능)
	getter(), setter()

상속
**생성자**
shape s = new circle
-> is a 관계

오버라이딩 vs 오버로딩
오버라이딩은 하위 클래스, 상위 클래스에서 모두 같다 (이름, 매개변수, 반환형, 내용 등등)
오버로딩은 이름만 같고 나머지가 다른 것

자바는 단일상속만을 지원한다

**생성자에서 초기화를 해주어야한다**

